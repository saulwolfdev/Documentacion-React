import { CodeBlock } from '../components-mdx/index.js';

# Props

Las "props" (propiedades) son una parte fundamental de la arquitectura de React.
Se utilizan para pasar datos desde un componente principal a sus componentes secundarios, lo que facilita la comunicación entre ellos y la personalización del comportamiento
de los componentes secundarios.

Ejemplo básico de uso de props:

Supongamos que tenemos un componente principal llamado "ParentComponent" y un componente secundario llamado "ChildComponent". Quieres pasar un mensaje del componente Padre al componente Hijo utilizando
props.

# ChildComponent.js

<CodeBlock
  codeString={`
    import React from 'react';

    export const ChildComponent = (props) => {
    // Acceda al "mensaje" recibido del componente principal
    return <p>{props.message}</p>;
    };
  `} 
  language='javascript'
/>

# ParentComponent.js

<CodeBlock
  codeString={`
    import React from 'react';
    import { ChildComponent } from './ChildComponent';

    export const ParentComponent = ( ) => {
    const message = "Hola desde el componente padre";

    // Pasamos la prop "mensaje" al componente Hijo
    return <ChildComponent message={message} />;
    };
  `} 
  language='javascript'
/>

# App.js

<CodeBlock
  codeString={`
    import React from 'react';
    import { ParentComponent } from './ParentComponent';

    const App = () => {
    return (
        <div>
        <h1>My App</h1>
        <ParentComponent /> //recibiremos el mensaje que se paso por las props: "Hola desde el componente padre"
        </div>
    );
    };

    export default App;
  `} 
  language='javascript'
  sandboxUrl="https://codesandbox.io/s/props-tdmrtr"
  showButtons= {true}
/>

En este ejemplo, hemos creado dos archivos separados, "ParentComponent" y "ChildComponent". Luego, en el archivo principal "App.js", importamos "ParentComponent" y lo utilizamos. 
Al hacer esto, estamos pasando la prop message al "ChildComponent", que es parte de la lógica interna del "ParentComponent".